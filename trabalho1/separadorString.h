# Ifndef SEPARADOR_STRING
# define SEPARADOR_STRING

void separatorUnify( char str[] ); //Função que formata o texto (Trab1 exe. 2.1)
int sizeString (char st[]); //Função que cálculo o tamanho da string
bool isParam (char c); //Função que valida carater a carater se é para substituir 
int utf8Length( char a[], int i ); //Função que devolve o nº de bytes de um caracter 

# endif

